version: '3'
networks:
  backend:
services:
  postgres:
    image: postgres
    container_name: pg-docker
    networks:
      backend:
        aliases: 
          - database
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
    volumes:
      - /tmp/pgdata:/var/lib/postgresql/data
      - /tmp/postgres.conf:/etc/postgresql/postgresql.conf
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    restart: always
